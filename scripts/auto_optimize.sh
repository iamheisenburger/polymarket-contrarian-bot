#!/bin/bash
# DEPRECATED — Use scripts/auto_manage.sh instead.
#
# The optimizer was over-engineered and caused losses by relaxing filters.
# The coin manager keeps strategy filters FIXED and only adapts which
# coins trade live vs paper.

echo "DEPRECATED: Use scripts/auto_manage.sh instead."
echo "The optimizer has been replaced by the coin manager."
exit 1
